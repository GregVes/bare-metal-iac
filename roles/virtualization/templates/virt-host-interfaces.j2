# set bridge for main network interface

auto lo
iface lo inet loopback

iface {{ main_interface }} inet manual

auto br0
iface br0 inet dhcp
        bridge_ports {{ main_interface }}
        bridge_stp off
        bridge_fd 0
        bridge_maxwait 0
